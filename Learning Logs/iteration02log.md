The hardest interaction to figure out was defenitly saving the different snapshots using local storage. To fix this, I had to convert the data to a string, them write it with localStorage.setItem, and load the string with localStorage.getItem, then lastley change it back into an object with JSON. I used arrays for the dropdown menu, the live asset list that the user has loaded, and the saved snapshot with previous asset lists. The other thing I really struggled with were the visualizations. A lot of the visualizations I began with were buggy, the CSS transitions were not smooth, and they often overlapped with other information on the page. 